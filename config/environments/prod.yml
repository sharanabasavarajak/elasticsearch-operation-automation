# Production Environment Configuration

elasticsearch:
  host: elasticsearch-prod.example.com
  port: 9200
  scheme: https
  # username: ${ES_USERNAME}
  # password: ${ES_PASSWORD}
  verify_certs: true

# Stop immediately on any errors in production
stop_on_error: true

report_file: ./reports/prod-execution-report.json
