# Performance Environment Configuration

elasticsearch:
  host: elasticsearch-perf.example.com
  port: 9200
  scheme: https
  # username: ${ES_USERNAME}
  # password: ${ES_PASSWORD}
  verify_certs: true

stop_on_error: true
report_file: ./reports/perf-execution-report.json
