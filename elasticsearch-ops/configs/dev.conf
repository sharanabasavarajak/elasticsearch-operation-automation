# Development Environment Configuration
# Simple key=value format

# Elasticsearch connection details
ES_HOST=elasticsearch-dev.example.com
ES_PORT=9200
ES_SCHEME=https

# Authentication (set via GitLab CI/CD variables for security)
# ES_USERNAME=dev_user
# ES_PASSWORD=dev_password

# SSL certificate verification (true/false)
VERIFY_CERTS=true

# Stop execution if any operation fails (true/false)
STOP_ON_ERROR=false
